# Flask
FLASK_ENV=development
SECRET_KEY=change-this-secret-key
JWT_SECRET_KEY=change-this-jwt-secret-key

# MySQL Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=users

# Admin credentials used only to issue JWT tokens (bonus auth)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Pagination default
DEFAULT_PAGE_LIMIT=10
